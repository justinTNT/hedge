// AUTO-GENERATED by src/Gen/Program.fs -- do not edit by hand.
module Alerts.Db

open Hedge.Workers

// ============================================================
// AlertSource (alerts_alert_sources)
// ============================================================

type AlertSourceRow = {
    Id: string
    Topic: string
    FeedUrl: string
    Enabled: bool
    CreatedAt: int
}

type AlertSourceCreate = {
    Topic: string
    FeedUrl: string
    Enabled: bool
}

let parseAlertSourceRow (row: obj) : AlertSourceRow =
    { Id = rowStr row "id"
      Topic = rowStr row "topic"
      FeedUrl = rowStr row "feed_url"
      Enabled = rowBool row "enabled"
      CreatedAt = rowInt row "created_at" }

let selectAlertSources (db: D1Database) : D1PreparedStatement =
    db.prepare("SELECT id, topic, feed_url, enabled, created_at FROM alerts_alert_sources ORDER BY created_at DESC LIMIT 100")

let selectAlertSource (id: string) (db: D1Database) : D1PreparedStatement =
    bind (db.prepare("SELECT id, topic, feed_url, enabled, created_at FROM alerts_alert_sources WHERE id = ?")) [| box id |]

let insertAlertSource (db: D1Database) (id: string) (now: int) (create: AlertSourceCreate) =
    let stmt =
        bind (db.prepare("INSERT INTO alerts_alert_sources (id, topic, feed_url, enabled, created_at) VALUES (?, ?, ?, ?, ?)"))
             [| box id; box create.Topic; box create.FeedUrl; box create.Enabled; box now |]
    {| Stmt = stmt; Id = id; CreatedAt = now |}

let updateAlertSource (id: string) (create: AlertSourceCreate) (db: D1Database) : D1PreparedStatement =
    bind (db.prepare("UPDATE alerts_alert_sources SET topic = ?, feed_url = ?, enabled = ? WHERE id = ?"))
         [| box create.Topic; box create.FeedUrl; box create.Enabled; box id |]

let deleteAlertSource (id: string) (db: D1Database) : D1PreparedStatement =
    bind (db.prepare("DELETE FROM alerts_alert_sources WHERE id = ?")) [| box id |]

// ============================================================
// PendingPost (alerts_pending_posts)
// ============================================================

type PendingPostRow = {
    Id: string
    SourceId: string
    EntryKey: string
    Title: string
    Link: string
    Snippet: string
    PublishedAt: int
    Approved: bool
    Rejected: bool
    OwnerComment: string
    CreatedAt: int
}

type PendingPostCreate = {
    SourceId: string
    EntryKey: string
    Title: string
    Link: string
    Snippet: string
    PublishedAt: int
    Approved: bool
    Rejected: bool
    OwnerComment: string
}

let parsePendingPostRow (row: obj) : PendingPostRow =
    { Id = rowStr row "id"
      SourceId = rowStr row "source_id"
      EntryKey = rowStr row "entry_key"
      Title = rowStr row "title"
      Link = rowStr row "link"
      Snippet = rowStr row "snippet"
      PublishedAt = rowInt row "published_at"
      Approved = rowBool row "approved"
      Rejected = rowBool row "rejected"
      OwnerComment = rowStr row "owner_comment"
      CreatedAt = rowInt row "created_at" }

let selectPendingPosts (db: D1Database) : D1PreparedStatement =
    db.prepare("SELECT id, source_id, entry_key, title, link, snippet, published_at, approved, rejected, owner_comment, created_at FROM alerts_pending_posts WHERE approved = 0 AND rejected = 0 ORDER BY published_at ASC LIMIT 500")

let selectPendingPost (id: string) (db: D1Database) : D1PreparedStatement =
    bind (db.prepare("SELECT id, source_id, entry_key, title, link, snippet, published_at, approved, rejected, owner_comment, created_at FROM alerts_pending_posts WHERE id = ?")) [| box id |]

let insertPendingPost (db: D1Database) (id: string) (now: int) (create: PendingPostCreate) =
    let stmt =
        bind (db.prepare("INSERT INTO alerts_pending_posts (id, source_id, entry_key, title, link, snippet, published_at, approved, rejected, owner_comment, created_at) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)"))
             [| box id; box create.SourceId; box create.EntryKey; box create.Title; box create.Link; box create.Snippet; box create.PublishedAt; box create.Approved; box create.Rejected; box create.OwnerComment; box now |]
    {| Stmt = stmt; Id = id; CreatedAt = now |}

let updatePendingPost (id: string) (create: PendingPostCreate) (db: D1Database) : D1PreparedStatement =
    bind (db.prepare("UPDATE alerts_pending_posts SET source_id = ?, entry_key = ?, title = ?, link = ?, snippet = ?, published_at = ?, approved = ?, rejected = ?, owner_comment = ? WHERE id = ?"))
         [| box create.SourceId; box create.EntryKey; box create.Title; box create.Link; box create.Snippet; box create.PublishedAt; box create.Approved; box create.Rejected; box create.OwnerComment; box id |]

let deletePendingPost (id: string) (db: D1Database) : D1PreparedStatement =
    bind (db.prepare("DELETE FROM alerts_pending_posts WHERE id = ?")) [| box id |]

let selectPendingPostsBySourceId (sourceId: string) (db: D1Database) : D1PreparedStatement =
    bind (db.prepare("SELECT id, source_id, entry_key, title, link, snippet, published_at, approved, rejected, owner_comment, created_at FROM alerts_pending_posts WHERE source_id = ? ORDER BY created_at DESC LIMIT 100")) [| box sourceId |]

// ============================================================
// Promotion (alerts_promotions)
// ============================================================

type PromotionRow = {
    Id: string
    EntryKey: string
    ItemId: string
    CreatedAt: int
}

type PromotionCreate = {
    EntryKey: string
    ItemId: string
}

let parsePromotionRow (row: obj) : PromotionRow =
    { Id = rowStr row "id"
      EntryKey = rowStr row "entry_key"
      ItemId = rowStr row "item_id"
      CreatedAt = rowInt row "created_at" }

let selectPromotions (db: D1Database) : D1PreparedStatement =
    db.prepare("SELECT id, entry_key, item_id, created_at FROM alerts_promotions ORDER BY created_at DESC LIMIT 100")

let selectPromotion (id: string) (db: D1Database) : D1PreparedStatement =
    bind (db.prepare("SELECT id, entry_key, item_id, created_at FROM alerts_promotions WHERE id = ?")) [| box id |]

let insertPromotion (db: D1Database) (id: string) (now: int) (create: PromotionCreate) =
    let stmt =
        bind (db.prepare("INSERT INTO alerts_promotions (id, entry_key, item_id, created_at) VALUES (?, ?, ?, ?)"))
             [| box id; box create.EntryKey; box create.ItemId; box now |]
    {| Stmt = stmt; Id = id; CreatedAt = now |}

let updatePromotion (id: string) (create: PromotionCreate) (db: D1Database) : D1PreparedStatement =
    bind (db.prepare("UPDATE alerts_promotions SET entry_key = ?, item_id = ? WHERE id = ?"))
         [| box create.EntryKey; box create.ItemId; box id |]

let deletePromotion (id: string) (db: D1Database) : D1PreparedStatement =
    bind (db.prepare("DELETE FROM alerts_promotions WHERE id = ?")) [| box id |]

module Tables =
    let alertSource = "alerts_alert_sources"
    let pendingPost = "alerts_pending_posts"
    let promotion = "alerts_promotions"