// AUTO-GENERATED by src/Gen/Program.fs -- do not edit by hand.
module Server.Routes

open Fable.Core
open Thoth.Json
open Hedge.Workers
open Hedge.Router
open Codecs
open Server.Env

let dispatch (request: WorkerRequest) (env: Env) (ctx: ExecutionContext)
    : JS.Promise<WorkerResponse> option =
    let route = parseRoute request
    match route with
    | GET path when matchPath "/api/site" path = Some (Exact "/api/site") ->
        Some (Server.Handlers.getSite env)

    | GET path ->
        match matchPath "/api/page/:id" path with
        | Some (WithParam (_, id)) -> Some (Server.Handlers.getPage id env)
        | _ ->
        None

    | _ -> None
