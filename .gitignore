# Dependencies
node_modules/

# Build outputs
dist/
build/
_site/
.fable/

# Extension packaged builds
apps/microblog/extension/dist-*/
apps/microblog/extension/*.zip
# Fable intermediate output — regenerated by `npm run build:extension`, never a build input
apps/microblog/extension/fable_output/

# IDE
.idea/
*.swp
*.swo
.vscode/

# .NET
bin/
obj/
*.user
*.suo

# Cloudflare
.wrangler/
.dev.vars

# Extension config (contains admin keys)
apps/microblog/extension/sites.json

# Local secrets
/private/

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*

# rich-text lib is copied from packages/rich-text at build time
apps/*/lib/rich-text/

# archive has a vanilla-JS client (no Client.fsproj), so the ClientGen.fs that
# `npm run gen` still emits is dead output — never compiled, never committed.
apps/archive/src/Client/generated/

# guest-session.js is copied from packages/hedge/lib at build time (prep:lib);
# generated per app, so ignore it everywhere — except music, which carries its
# own slimmer, divergent copy as tracked source.
apps/*/lib/guest-session.js
!apps/music/lib/guest-session.js
